1、javascript数据类型与运算符	-a. 五种基本数据类型:null, undefined, number, string, boolean	-b. 一种对象类型:object		-c. 四则运算,取模运算(%),逻辑运算,关系运算		数据类型之间的转换是各种数据类型之间运算的基础			2、javascript流程控制		流程控制就是控制程序执行的流程		-a、顺序		-b、选择		-c、循环		数据类型，运算符与流程控制是所有计算机语言的基础语法。3、javascript内置对象		-a. Number包装函数	-b. String包装函数	-c. Boolean包装函数	-d. Date对象	-e. Array函数	-f. Math对象	-g. RegExp函数	-f. Function对象	-g. 自定义对象	4、javascript DOM操作	-a. document对象			document对象主要在整个dom文档中进行精确元素筛选，元素构建。提供了一些精准定位元素的方法和属性			eg: getElementById(), getElementsByTagName(), getElementsByName(),getElementsByClassName();				forms, all , images, links			-b. element对象			element对象，每一个节点，每一个标签，每一个元素都是一个元素对象，我们可以对该节			点对象进行属性访问，内容修改。通过属性赋值的方式绑定一个事件,事件内部与元素无关.							元素对象也支持在整个dom文档中对其他元素进行精确定位，通过元素的上级搜索，下级搜索，水平搜索				上级搜索：parentNode				下级搜索: childNodes, children, firstChild, lastChild				水平搜索: previousSibling, nextSibling				-c. event对象			当进入事件对象内部，就由事件对象做主，事件对象可以对外部标签进行访问，可以取得激发此事件的元素，			事件的类型，和事件发生时的鼠标，键盘状态信息								事件执行的三个阶段.捕获，目标，冒泡					通过增加监听的方式来控制事件的执行时机					一般不需要修改事件执行时机，了解三个阶段，可以更加细粒度的为元素进行事件绑定				5、javascriptBOM操作	-a. window对象		-b. open对象		-c. location对象		-d. history对象		-c. screen对象		-e. navigator对象